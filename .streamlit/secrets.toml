# Les variables d'environnement doivent être configurées dans Streamlit Cloud
# Accédez à: https://share.streamlit.io/deploy-settings

# Exemple de variables à configurer:
# mistral_api_key = "votre-clé-ici"
# database_url = "postgresql://..."

# Notes:
# - Ce fichier ne doit pas être commité
# - Les secrets réels doivent être définis via l'interface Streamlit Cloud
# - Les variables d'environnement locales (.env) seront lues par python-dotenv
